@using PagedList
@using PagedList.Mvc;
@model IPagedList<IShop.Core.leaves_Types>


@{
    ViewBag.Title = "Index";
}
<style>
    body {
        background-image: url(../../images/33.jpg);
        background-size: cover;
        font-family: Calibri
    }

    .table-hover > tbody > tr:hover {
        background-color: #D2D2D2;
    }

    table {
        border-collapse: collapse;
        overflow: visible;
    }

    .form-rounded2 {
        border-radius: 0.5rem;
        width: 20%;
        border-color: white;
    }
</style>

<div class="card " style="background-color: rgb(255, 255, 255, 0.61);">


    <div class="card-header" style="background-color: #0656A5">
        <h3 class="card-title" style="color:white">
            <i class="fa fa-umbrella"></i>
            Leave Types
        </h3>




        <div style="text-align: right; color: white ">
            @using (Html.BeginForm("Index", "leaves_Types", FormMethod.Get))
            {






                <a style="font-family: Calibri"> Leave Name</a>
                @Html.TextBox("search", null, new { @class = "form-rounded2", @PlaceHolder = " Leave Name" })
                <button type="submit" value="search" class="btn btn-sm btn-warning">
                    <i class="fas fa-search"></i>
                </button>

                @Html.ActionLink(" Add  Leave Type ", "Create", "leaves_Types", null, new { @class = "btn btn-sm btn-default btn" })


            }


        </div>
    </div>
    <div class="card-body">





        @if (TempData["Msg"] != null)
        {<div class="row">
                <div class="alert alert-success col-md-6 col-md-offset-4" align="center">
                    <button type="button" class="close" data-dismiss="alert" aria-label="Close"><span aria-hidden="true">&times;</span></button>
                    @TempData["Msg"]
                </div>
            </div>
        }

        <script>
            window.setTimeout(function () {
                $(".alert").fadeTo(500, 0).slideUp(500, function () {
                    $(this).remove();
                });
            }, 2000);
        </script>






        <div class="row">
            @foreach (var item in Model)
            {
                

                <div class="col-md-3 col-sm-6 col-6">
                    <div class="info-box" style="border-radius: 16px; background-color: #0656A5">


                        <div class="info-box-content" style="color:white">
                            <span class="info-box-text text-wrap"><h6><i class="fa fa-umbrella"></i>  .@item.Type :
                                <span class="badge badge-primary">  @item.Days Days </span></h6></span>
                            <span class="info-box-text text-wrap"><small><i class="fa fa-calendar"></i>  .@item.CreatedOn.ToString("dd-MMMM-yyyy")</small></span>
                            <div class="btn-group dropdown dropright">
                                <button type="button" class="btn btn-xs btn-warning dropdown-toggle" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                                    <i class="fa fa-paper-plane"></i>  . Action
                                </button>
                                <div class="dropdown-menu dropdown-menu-lg dropdown-menu-right">
                                    <a href="@Url.Action("Edit", "leaves_Types", new { id = item.Id })" class="dropdown-item">
                                        <i class="fa fa-pen-nib text-primary"></i> Update leave
                                    </a>
                                    <div class="dropdown-divider"></div>
                                    <a href="@Url.Action("Delete", "leaves_Types", new { id = item.Id })" class="dropdown-item">
                                        <i class="fa fa-trash text-danger"></i> Drop leave Type
                                    </a>

                                </div>
                            </div>
                            <!-- /.info-box-content -->
                        </div>
                    </div><!-- /.info-box -->
                </div>

            }
        </div>
        <style>
            .ul.pagination {
                display: inline-block;
                padding: 1;
                margin: 1;
                border-radius: 0.5rem;
            }

            ul.pagination li {
                display: inline;
            }

                ul.pagination li a {
                    color: black;
                    float: left;
                    padding: 8px 16px;
                    text-decoration: none;
                    transition: background-color;
                    background-color: #ddd;
                    border-color: white;
                    border-radius: 0.5rem;
                }

                    ul.pagination li a.active {
                        background-color: dodgerblue;
                        color: white;
                    }

                    ul.pagination li a:hover:not(.active) {
                        background-color: dodgerblue;
                        border-radius: 0.5rem;
                    }
        </style>
        <div id="container" style="text-align:left">

            <div class="pagination" style="text-align:left">
                @Html.PagedListPager(Model, page => Url.Action("Index", new { page, searchBy = Request.QueryString["SearchBy"], search = Request.QueryString["Search"] })
              , new PagedListRenderOptions() { Display = PagedListDisplayMode.IfNeeded, DisplayLinkToIndividualPages = true })
            </div>
        </div>


    </div>
